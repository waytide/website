# `reference-material/` gains a README saying what it holds — material the site will carry that is not yet a page, which is neither `content/`, since Hugo publishes that, nor a record directory, since nothing here states what happened — and naming the two properties of what belongs: it describes rather than binds, and it is hand-maintained until both surfaces are driven from one source
